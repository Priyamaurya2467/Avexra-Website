export const shadows = {
  // Subtle elevation (cards, sections)
  card: "0 4px 6px -1px rgba(0,0,0,0.05), 0 2px 4px -1px rgba(0,0,0,0.03)",

  // Hover state for cards
  cardHover:
    "0 20px 25px -5px rgba(0,0,0,0.05), 0 10px 10px -5px rgba(0,0,0,0.02)",

  // Glass panels (navbar, floating UI)
  glass: "0 10px 30px -5px rgba(0,0,0,0.08)",

  // Brand glow (CTAs, highlights)
  brandGlow: "0 0 40px -10px rgba(147,51,234,0.45)",

  // Strong hero glow (workflow canvas, featured cards)
  heroGlow: "0 25px 60px -15px rgba(59,111,226,0.35)",

  // Inner inset (inputs, panels)
  inset: "inset 0 0 20px rgba(255,255,255,0.6)",
};
